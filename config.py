"""
Конфигурация бота
Токен берётся из переменной окружения или указывается напрямую здесь
"""

import os

# --- ВАЖНО: вставьте сюда токен вашего бота, полученный у @BotFather ---
# Либо задайте переменную окружения BOT_TOKEN в системе
BOT_TOKEN = os.getenv("BOT_TOKEN")
if not BOT_TOKEN:
    raise ValueError("Переменная окружения BOT_TOKEN не задана!")

# Имя вашей донерной (отображается в приветствии)
RESTAURANT_NAME = "Донерная «ДонерХаус»"

# Номер телефона или способ связи для заказов (отображается при оформлении)
CONTACT_INFO = "📞 +7 (999) 123-45-67"
# --- Админские настройки ---
# По-умолчанию переменная ADMIN_ID используется для обратной совместимости — укажите
# реальный числовой Telegram user id администратора или chat_id группы (для группы: отрицательное число).
ADMIN_ID = int(os.getenv("ADMIN_ID", "5310770517"))
# Отдельно можно указать id чата, куда приходят уведомления (например группа). По-умолчанию — ADMIN_ID
# По умолчанию устанавливаем ваш переданный chat id: -5279377245
ADMIN_CHAT_ID = int(os.getenv("ADMIN_CHAT_ID", str(-5279377245)))
# ID пользователя-администратора, который может подтверждать платежи (по-умолчанию — ADMIN_ID)
# По умолчанию устанавливаем ваш переданный admin user id: 778497367
ADMIN_USER_ID = int(os.getenv("ADMIN_USER_ID", str(778497367)))

# Информация об оплате (отобразится пользователю)
KASPI_NUMBER = os.getenv("KASPI_NUMBER", "+7 (700) 000-00-00")
HALYK_CARD = os.getenv("HALYK_CARD", "KZ00 0000 0000 0000")
